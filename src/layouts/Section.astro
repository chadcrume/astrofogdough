---

/* 
props ideas:
- size
- align
- bg
- border
- columns
*/
interface Props {
  class?: string | undefined;
  id?: string | undefined;
  type?: string | undefined; // Not used but as an example
}
const { class: addClass, id }: Props = Astro.props;
---

<section
  id={id}
  class:list={["page-section", addClass]}
>
  <slot />
</section>
