---
import Card from "@components/Card.astro";
const { title, content, class:addClass , icon } = Astro.props;

// update to handle a UL?
---    



<Card title={title} class:list={[ 'items-center text-center', addClass ]}>     
  <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-12 mt-14">
    <slot />
  </div>
</Card>